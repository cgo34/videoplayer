export interface AdSlot {
    startTime: number;  // Le temps de début du slot publicitaire en secondes
    endTime: number;    // Le temps de fin du slot publicitaire en secondes
    content: string;    // Le contenu de la publicité (URL, ID, etc.)
  }
  